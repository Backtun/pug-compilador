## Compilador de código Pug a HTML

Proyecto que contiene un compilador para convertir código pug a html, y visualizar los cambios. También se realizan pruebas de código (pug) para entender su funcionamiento.

Ejecutar comando

````
npm install
````
